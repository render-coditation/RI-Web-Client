import React from 'react'
import { ProfilePicImage } from '../style'

const ProfileIcon = ({ profileImage }: any) => (
  <>
    <ProfilePicImage src={profileImage} alt='' />
  </>
)

export default ProfileIcon

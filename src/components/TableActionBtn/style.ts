import styled from 'styled-components'
import { ROUNDED_CIRCLE } from '../../constants/style/borders'

export const TableActionBtnContainer = styled.button`
  outline: none;
  border: none;
  border-radius: ${ROUNDED_CIRCLE};
`
